import { NameOfSinglePipe } from './name-of-single.pipe';

describe('NameOfSinglePipe', () => {
  it('create an instance', () => {
    const pipe = new NameOfSinglePipe();
    expect(pipe).toBeTruthy();
  });
});
